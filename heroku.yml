build:
  docker:
    web: Dockerfile
    worker:
      dockerfile: Dockerfile
      target: builder
    config: build/db/.env 
release:
  image: worker
  command:
    - make up_migrate_prod
run:
  web: /main
